Web
======

This will be set as the entry point for the nginx server and is configured as an override to the default 'public' folder.

Use [PHP Constants](https://craftcms.com/docs/php-constants) to configure as much as possible.
